/**
 * Firma şikayeti formu — iş kuralları (ileride Supabase ile senkron tutulabilir).
 * Telefon: `true` yapıldığında şema ve etiketler zorunlu alana döner.
 */
export const FIRM_COMPLAINT_PHONE_REQUIRED = false;
