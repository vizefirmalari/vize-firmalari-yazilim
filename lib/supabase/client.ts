/**
 * Tarayıcı Supabase istemcisi — `browser.ts` ile aynı fabrika.
 * İstek üzerine `createSupabaseClient` takma adı.
 */
export { createSupabaseBrowserClient } from "./browser";
export { createSupabaseBrowserClient as createSupabaseClient } from "./browser";
